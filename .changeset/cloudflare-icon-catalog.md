---
"@cloudflare/kumo": patch
---

Expand the repo-owned Cloudflare icon catalog, finalize the public `CloudflareIcon` API (including root/deep exports, generated glyph-name exports, and the shared sprite URL/variant metadata), and restore fresh-clone TypeScript resolution for the generated `ai/schemas` module.
