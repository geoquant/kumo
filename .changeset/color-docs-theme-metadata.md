---
"@cloudflare/kumo": patch
---

Export theme metadata for docs and tooling consumers, make generated theme CSS rely on mode-scoped variables instead of `light-dark()`, and update semantic token consumers accordingly.
