---
layout: ~/layouts/MdxDocLayout.astro
title: "Surface (Deprecated)"
description: "Deprecated compatibility wrapper. Prefer LayerCard for new surface-style containers."
sourceFile: "components/surface"
---

import ComponentExample from "~/components/docs/ComponentExample.astro";
import ComponentSection from "~/components/docs/ComponentSection.astro";
import Heading from "~/components/docs/Heading.astro";
import PropsTable from "~/components/docs/PropsTable.astro";
import {
  SurfaceDemo,
  SurfaceRenderDemo,
  SurfaceNestedDemo,
} from "~/components/demos/SurfaceDemo";

{/* Hero Demo */}

<ComponentSection>
  <ComponentExample demo="SurfaceDemo">
    <SurfaceDemo client:visible />
  </ComponentExample>
</ComponentSection>

{/* Installation */}

<ComponentSection>
  <Heading level={2}>Deprecation</Heading>
  <p class="mb-4 text-kumo-strong">
    <code>Surface</code> is deprecated. Prefer <a href="/components/layer-card" class="text-kumo-link hover:underline">LayerCard</a> for new work.
    Existing <code>Surface</code> usage remains supported for backwards compatibility.
  </p>

```tsx
import { LayerCard } from "@cloudflare/kumo";

export default function Example() {
  return <LayerCard className="rounded-lg p-6">Card content</LayerCard>;
}
```

</ComponentSection>

{/* render prop */}

<ComponentSection>
  <Heading level={2}>Rendering as a different element</Heading>
  <p class="text-kumo-strong mb-4">
    The deprecated <code>Surface</code> API still supports the <code>render</code>
    prop, and keeps <code>as</code> as a deprecated migration alias. New
    <code>LayerCard</code> usage should use <code>render</code> only.
  </p>
  <ComponentExample demo="SurfaceRenderDemo">
    <SurfaceRenderDemo client:visible />
  </ComponentExample>
</ComponentSection>

{/* Nested Surfaces */}

<ComponentSection>
  <Heading level={2}>Migration notes</Heading>
  <p class="text-kumo-strong mb-4">
    If you previously used <code>Surface</code> to avoid wrapping content in
    <code>LayerCard.Primary</code>, you can now render content directly inside
    <code>LayerCard</code> instead.
  </p>
  <ComponentExample demo="SurfaceNestedDemo">
    <SurfaceNestedDemo client:visible />
  </ComponentExample>
</ComponentSection>

{/* API Reference */}

<ComponentSection>
  <Heading level={2}>API Reference</Heading>
  <PropsTable component="Surface" />
</ComponentSection>
