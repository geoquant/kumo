/**
 * Kumo Component Registry
 *
 * Type definitions and utilities for the component registry.
 * The registry provides AI-readable component metadata from TypeScript types and demo files.
 */

export type {
  ComponentBehaviorEvent,
  ComponentBehaviorManifest,
  ComponentBehaviorSchema,
  ComponentBehaviorSupport,
  ComponentBehaviorValidationHints,
  ComponentRegistry,
  ComponentSchema,
  ComponentStyling,
  ComponentType,
  GenerativeSupportStatus,
  LayoutRole,
  PropSchema,
  SubComponentSchema,
  WrapperKind,
} from "./types";
