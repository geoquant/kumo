## Goal

Use 3 `review` subagents to review current branch + any uncommitted changes, then correlate findings. User also asked to commit uncommitted changes first.

## Current State (read-only checks)

- VCS: `git` (`.git/` present, no `.jj/`)
- Branch: `kumo-localization`
- Divergence vs `main`: `0 behind / 27 ahead`
- Working tree: clean (`0` staged, `0` unstaged, `0` untracked)

## Recommended Execution Plan

1. Preflight VCS detect
   - Check `.jj/`; if present use `jj`, else `git`.
   - Capture baseline: branch, ahead/behind, status.

2. Commit-first step (as requested)
   - Run status/diff/log checks.
   - If uncommitted changes exist:
     - Stage relevant files (exclude secrets).
     - Draft commit message from diff intent.
     - Commit.
     - Re-check status to confirm clean tree.
   - If no uncommitted changes exist (expected now):
     - Skip commit and record: `No uncommitted changes; commit step skipped`.

3. Review scope setup
   - Scope = all branch changes since divergence: `main...HEAD`.
   - Include working tree diff only if present after step 2.

4. Run 3 `review` subagents in parallel
   - Reviewer A: correctness/reliability/regression risk.
   - Reviewer B: architecture/maintainability/API consistency.
   - Reviewer C: tests/security/release readiness.

5. Correlate outputs
   - Merge + dedupe by root cause (`file + symptom + cause`).
   - Mark consensus: `1/3`, `2/3`, `3/3`.
   - Rank by severity + confidence.
   - Flag conflicts as `Needs decision` when not evidence-resolvable.

6. Final deliverable format
   - Commit Status (done/skipped + reason)
   - Scope Reviewed (`main...HEAD`, commit count)
   - Top Risks (P0/P1)
   - Correlated Findings table:
     - `ID | Severity | Consensus | Confidence | File:Line | Issue | Evidence | Fix`
   - Conflicts/Disagreements
   - Quick wins
   - Appendix: raw A/B/C findings

## Verification

- Confirm commit state via status after commit-step logic.
- Confirm review coverage includes all commits in `main...HEAD`.
- Ensure each finding has file evidence and severity.
- Ensure dedupe removed duplicates and preserved disagreements.

## Unresolved Questions

- None.
