# Code Review: `geoquant/streaming-ui` Branch

**Branch:** 243 commits, 277 files changed, ~53K lines added  
**Uncommitted:** 10 files — auth-removal refactor (clean, complete)

---

## Correlated Findings (3 reviewers)

### CRITICAL (fix before merge)

| #   | Issue                                                                                                                 | Files                                               | Reviewers |
| --- | --------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- | --------- |
| C1  | **RFC6902 `as unknown as` double-casts on LLM data** — malformed patch values silently corrupt UI tree                | `src/streaming/rfc6902.ts:41-47,155,172,184`        | R1        |
| C2  | **`openExternal` has no URL scheme validation** — `javascript:` injection possible if called outside `dispatchAction` | `_PlaygroundPage.tsx:374`, `_StreamingDemo.tsx:349` | R2        |
| C3  | **Rate limiting bypassed when `cf-connecting-ip` absent** — skipped entirely (not just in dev)                        | `api/chat/index.ts:166-180`                         | R2        |
| C4  | **Phantom workspace entry** — `_examples/kumo-stream` in pnpm-workspace.yaml doesn't exist                            | `pnpm-workspace.yaml:3`                             | R3        |
| C5  | **Non-null assertion in registry codegen** — `BLOCKS.find(...)!` will throw if no match                               | `scripts/component-registry/index.ts:774`           | R3        |

### HIGH (fix soon after merge)

| #   | Issue                                                                               | Files                                         | Reviewers |
| --- | ----------------------------------------------------------------------------------- | --------------------------------------------- | --------- |
| H1  | **`ComponentType<any>`** explicit any in component map                              | `src/generative/component-map.ts:40`          | R1        |
| H2  | **Module-level mutable state** in catalog creates race conditions                   | `src/catalog/catalog.ts:43-48`                | R1        |
| H3  | **Inconsistent `isRecord`** — ui-tree-renderer version accepts arrays, others don't | `src/generative/ui-tree-renderer.tsx:833-835` | R1        |
| H4  | **Multiple unsafe `as` casts in request parsing**                                   | `api/chat/index.ts:109-146`                   | R2, R3    |
| H5  | **Unsafe `as Baseline` on user-uploaded JSON** — no deep validation                 | `_EvalReportPage.tsx:845`                     | R2        |
| H6  | **Unsafe `as` casts in PlaygroundPage helpers**                                     | `_PlaygroundPage.tsx:162,170,283,1518`        | R2        |

### MEDIUM

| #   | Issue                                                                     | Files                                                                    | Reviewers |
| --- | ------------------------------------------------------------------------- | ------------------------------------------------------------------------ | --------- |
| M1  | **O(n^2) string concat** in `setRawJsonl` state updates during streaming  | `_PlaygroundPage.tsx:487`                                                | R2        |
| M2  | **Heavy code duplication** between PlaygroundPage and StreamingDemo       | `_PlaygroundPage.tsx`, `_StreamingDemo.tsx`                              | R2        |
| M3  | **9-pass normalization on every patch** — expensive for fast streaming    | `ui-tree-renderer.tsx:1258-1278`                                         | R1        |
| M4  | **Rate limit 20->100 req/min for anonymous** — 5x cost amplification risk | `wrangler.jsonc:21-29`                                                   | R3        |
| M5  | **Raw Tailwind `neutral-*` colors** in DemoButton (convention violation)  | `DemoButton.tsx:83-84`                                                   | R2        |
| M6  | **Undefined `shimmer` keyframe** referenced in animation style            | `_PlaygroundPage.tsx:648`                                                | R2        |
| M7  | **No URL length limit** in sanitizeUrl — LLM could generate multi-MB URLs | `src/streaming/url-policy.ts:19-38`                                      | R1        |
| M8  | **Duplicate section header comment** (leftover from auth removal)         | `_PlaygroundPage.tsx:248-254`                                            | R2, R3    |
| M9  | **Stale specs** reference removed PLAYGROUND_SECRET auth                  | `specs/eval-pipeline-baseline.md:75`, `specs/streaming-playground.md:24` | R3        |
| M10 | **Non-null assertions** (`!`) scattered in parser and graders             | `jsonl-parser.ts:57`, `structural-graders.ts:197`, `index.ts:328,347`    | R1, R3    |

### LOW

| #   | Issue                                                                   | Reviewers |
| --- | ----------------------------------------------------------------------- | --------- |
| L1  | System prompt exposed unauthenticated at `/api/chat/prompt`             | R2        |
| L2  | Array index as React key in multiple list renders                       | R2        |
| L3  | Unused `preserveDirectives` import in vite.config                       | R3        |
| L4  | CI doesn't typecheck docs-astro package                                 | R3        |
| L5  | Two changesets for same breadcrumbs fix                                 | R3        |
| L6  | `process-action-result.ts:39` — dead `w.opener = null` after `noopener` | R1        |
| L7  | Large system prompt (~15KB+) default includes all 8 examples            | R1        |

### TEST GAPS (flagged by R1)

- `runtime-value-store.test.ts` — only 3 cases; missing subscribe/unsubscribe, error swallowing, concurrent modification
- `hooks.ts` — no test for `batchPatches` RAF throttling or cleanup-on-unmount
- `ui-tree-renderer.tsx` normalization passes — no direct unit tests for 9 normalization functions
- `url-policy.test.ts` — only 4 cases; missing case-insensitive schemes, credentials in URLs, very long URLs

---

## Implementation Progress

### DONE (all edits in working tree, uncommitted)

| ID    | File(s)                                                               | What was done                                                                                                                                                                       |
| ----- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1    | `src/streaming/rfc6902.ts`                                            | Added `isRecord`, `isElementLike`, `isElementsMapLike` runtime guards; replaced all unsafe `as` casts with validation                                                               |
| C2    | `_PlaygroundPage.tsx:374`, `_StreamingDemo.tsx:349`                   | Added `new URL()` scheme validation in `openExternal` callbacks                                                                                                                     |
| C3    | `api/chat/index.ts:163-190`                                           | Added `x-forwarded-for` fallback + fail-closed in prod when no IP header                                                                                                            |
| C4    | `pnpm-workspace.yaml:3`                                               | Removed phantom `_examples/kumo-stream` entry                                                                                                                                       |
| C5    | `scripts/component-registry/index.ts:774`                             | Replaced `!` with guard + `continue`                                                                                                                                                |
| H1    | `component-map.ts:40`                                                 | `ComponentType<any>` ��� `ComponentType<Record<string, unknown>>`                                                                                                                   |
| H2    | `ui-tree-renderer.tsx:833`                                            | Added `&& !Array.isArray(value)` to `isRecord`                                                                                                                                      |
| H3/H4 | `api/chat/index.ts:107-149`                                           | Rewrote `parseChatRequest` with `isPlainObject`/`isHistoryEntry` type guards, eliminated all `as` casts                                                                             |
| H5/H6 | `_PlaygroundPage.tsx:158-172,281-285,1522-1533`                       | Replaced all `as` casts with `in`-check narrowing; added `isSkillMeta` type guard + `.filter(isSkillMeta)` to eliminate `as SkillMeta[]`                                            |
| M1    | `_PlaygroundPage.tsx:334,448,492-506`                                 | Replaced O(n²) per-token `setRawJsonl(prev + token)` with `useRef` accumulator, single flush after stream                                                                           |
| M7    | `src/streaming/url-policy.ts:22`                                      | Added `if (trimmed.length > 8192) return { ok: false, reason: "too-long" }`                                                                                                         |
| M8    | `_PlaygroundPage.tsx:248-250`                                         | Deleted duplicate `// Skills hook` section header                                                                                                                                   |
| M9    | `specs/eval-pipeline-baseline.md`, `specs/streaming-playground.md`    | Removed all stale `PLAYGROUND_SECRET`/`X-Playground-Key`/`--playground-key` refs; updated auth sections to reflect open access with IP rate limiting; struck through D6 deliverable |
| M10   | `jsonl-parser.ts:57`, `structural-graders.ts:197`, `index.ts:328,347` | Replaced `!` assertions with `?? ""`/null guards/`continue`                                                                                                                         |
| L3    | `vite.config.ts:6`                                                    | Removed unused `preserveDirectives` import                                                                                                                                          |
| L6    | `process-action-result.ts:35-40`                                      | Removed dead `w.opener = null` after `noopener`                                                                                                                                     |

### REMAINING

None — all actionable items complete. Typecheck passes (0 errors). All 1135 tests pass (48 files, 0 failures).

### CANCELLED (verified false positives)

- **M5** (DemoButton `neutral-*`) — intentional for conic-gradient demo, not a convention violation
- **M6** (shimmer keyframe) — defined in `global.css:149-155`
- **SelectDemo merge conflict** — stale LSP error, no conflict markers in file

---

## Strengths (all 3 reviewers agreed)

1. **Security posture is excellent**: prototype pollution prevention, URL sanitization allowlist, blocked dangerous props, defense-in-depth
2. **LLM output resilience**: truncated JSON repair, enum coercion, 9-pass normalization, graceful degradation
3. **Clean module boundaries**: streaming (pure), generative (rendering), catalog (prompts) — independently testable
4. **Immutability discipline**: `applyPatch` returns new objects, `readonly` interfaces, `Object.freeze`
5. **Well-modeled ADTs**: `ActionResult`, `UrlSanitizationResult` as discriminated unions
6. **Auth removal is thorough**: no leftover references to removed auth system
7. **Drift detection tests**: excellent infra-level testing model
