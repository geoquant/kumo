# Progress Log
PRD: kumo-app-runtime
Started: 2026-03-07

## Codebase Patterns
- Hard rule: keep every task on `geoquant/streaming-ui`; do not branch this work elsewhere.
- New public Kumo subpaths need four touchpoints: source barrel, Vite build entry, `package.json` export, and an import test.
- In `app-runtime/core`, keep path utils, expression resolution, store behavior, and action execution in separate pure modules with focused tests.
- For new runtime semantics, prefer pure snapshot builders (`expandAppSpec`), pure evaluators (`validateFieldsForMode`), and explicit reaction loops (`runWatchers`) over renderer-coupled logic.
- Keep the patch engine spec-agnostic: one RFC 6902 implementation should handle both legacy `UITree` docs and new `AppSpec` docs, with normalization and repair in pure helpers above render.
- RFC 6902 `replace` should not be used to create missing keys in follow-on tests or adapters; use `add` when populating new props or state paths.

---

## Task - core-1
- What was implemented: added the experimental app runtime type surface with `AppSpec`, app elements, expressions, actions, repeat, validation, watchers, store contracts, and compatibility aliases for legacy `UITree` consumers.
- Files changed: `packages/kumo/src/app-runtime/core/types.ts`, `packages/kumo/src/app-runtime/core/index.ts`, `packages/kumo/src/app-runtime/index.ts`, `packages/kumo/src/index.ts`, `packages/kumo/package.json`, `packages/kumo/vite.config.ts`, `packages/kumo/tests/imports/app-runtime.test.ts`, `.changeset/app-runtime-core-types.md`, `.opencode/state/kumo-app-runtime/prd.json`.
- Learnings: the least risky migration boundary is a compatibility alias first; for Kumo packaging, a new runtime surface is not real until entry files, package exports, and import coverage all land together.

## Task - runtime-1
- What was implemented: added pure app runtime modules for immutable store updates, JSON Pointer path handling, shared expression resolution, binding writes through the app store, and sequential built-in action execution for state, list, form, and navigation flows.
- Files changed: `packages/kumo/src/app-runtime/core/path.ts`, `packages/kumo/src/app-runtime/core/store.ts`, `packages/kumo/src/app-runtime/core/expressions.ts`, `packages/kumo/src/app-runtime/core/actions.ts`, `packages/kumo/src/app-runtime/core/index.ts`, `packages/kumo/src/app-runtime/index.ts`, `packages/kumo/src/index.ts`, `packages/kumo/tests/app-runtime/actions.test.ts`, `packages/kumo/tests/app-runtime/expressions.test.ts`, `packages/kumo/tests/app-runtime/store.test.ts`, `.opencode/state/kumo-app-runtime/prd.json`.
- Learnings: action sequencing should recompute expression context from the store before each step so later steps see fresh state; binding writes are simplest when ref sources resolve to JSON Pointer targets before touching the store.

## Task - runtime-2
- What was implemented: added pure repeat expansion, validation-by-mode, and watcher reaction modules so repeated rows derive from array state, field rules run on change/blur/submit, and state-change watchers execute actions with cycle protection.
- Files changed: `packages/kumo/src/app-runtime/core/repeat.ts`, `packages/kumo/src/app-runtime/core/validation.ts`, `packages/kumo/src/app-runtime/core/watchers.ts`, `packages/kumo/src/app-runtime/core/index.ts`, `packages/kumo/src/app-runtime/index.ts`, `packages/kumo/src/index.ts`, `packages/kumo/tests/app-runtime/repeat.test.ts`, `packages/kumo/tests/app-runtime/validation.test.ts`, `packages/kumo/tests/app-runtime/watchers.test.ts`, `.opencode/state/kumo-app-runtime/prd.json`.
- Learnings: repeat should produce a derived render snapshot plus scope map instead of mutating the authored spec; watchers are easiest to reason about when they run from an explicit previous-state baseline and hard-stop on bounded reaction depth.

## Task - streaming-1
- What was implemented: upgraded the RFC 6902 patch engine to support add, replace, remove, move, copy, and test across generic JSON documents, then added pure app-runtime normalization and repair helpers so nested authoring input flattens into `AppSpec` before render.
- Files changed: `packages/kumo/src/streaming/rfc6902.ts`, `packages/kumo/src/streaming/text-normalizer.ts`, `packages/kumo/src/app-runtime/core/normalize.ts`, `packages/kumo/src/app-runtime/core/repair.ts`, `packages/kumo/src/app-runtime/core/index.ts`, `packages/kumo/src/app-runtime/index.ts`, `packages/kumo/src/index.ts`, `packages/kumo/tests/streaming/rfc6902.test.ts`, `packages/kumo/tests/app-runtime/normalize.test.ts`, `.opencode/state/kumo-app-runtime/prd.json`.
- Learnings: full RFC 6902 support is easier to maintain when path traversal is generic and immutable instead of hard-coded to tree shape; flatten + repair belongs in reusable pre-render helpers so renderer code can stay focused on presentation.

## Task - react-1
- What was implemented: packaged React-facing app runtime hooks under `@cloudflare/kumo/app-runtime/react`, including `useUIStream` for patch/full-spec sessions, `useChatUI` for mixed text plus UI turns, and core store/watcher updates so apps can observe runtime effects and keep hook-driven sessions in sync.
- Files changed: `packages/kumo/src/app-runtime/react/index.ts`, `packages/kumo/src/app-runtime/react/types.ts`, `packages/kumo/src/app-runtime/react/use-ui-stream.ts`, `packages/kumo/src/app-runtime/react/use-chat-ui.ts`, `packages/kumo/src/app-runtime/core/actions.ts`, `packages/kumo/src/app-runtime/core/store.ts`, `packages/kumo/src/app-runtime/core/watchers.ts`, `packages/kumo/package.json`, `packages/kumo/vite.config.ts`, `packages/kumo/tests/app-runtime/use-ui-stream.test.tsx`, `packages/kumo/tests/app-runtime/use-chat-ui.test.tsx`, `packages/kumo/tests/imports/app-runtime-react.test.ts`, `packages/kumo/tests/app-runtime/store.test.ts`, `packages/kumo/tests/app-runtime/watchers.test.ts`, `packages/kumo/tests/loadable/loadable-tree-access.test.ts`, `packages/kumo/tests/loadable/loadable-batched-render.test.ts`, `packages/kumo/tests/streaming/jsonl-parser.test.ts`, `.opencode/state/kumo-app-runtime/prd.json`.
- Learnings: hook-managed app sessions need stable refs for in-flight assistant turns or repeated streaming callbacks will create duplicate placeholders; when app runtime hooks consume store snapshots, subscribe-driven state is safer than `useSyncExternalStore` against clone-on-read stores.
