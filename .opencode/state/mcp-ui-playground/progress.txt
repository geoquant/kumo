# Progress Log
PRD: mcp-ui-playground
Started: 2026-03-04
**CRITICAL: All work MUST be on branch `geoquant/streaming-ui`. Do NOT create new branches or use `main`.**

## Codebase Patterns
- `agents` pins `@modelcontextprotocol/sdk` at `1.23.0` — must match exact version or types diverge (private property clash in `Server`)
- Wrangler `pretypecheck` script generates `types/worker-configuration.d.ts` — file must NOT pre-exist or wrangler errors with "non-Wrangler worker-configuration.d.ts already exists"
- `pnpm-workspace.yaml` uses `packages/*` glob — new packages auto-register, use `catalog:` for shared deps
- McpAgent pattern: extend `McpAgent<Env, State>`, set `server = new McpServer(...)`, implement `async init()`, route via `ClassName.serve('/path', { binding })` in fetch handler
- `agents` bundles MCPClientManager which dynamically imports `ai` — must add `ai` as dependency for wrangler dev to bundle without errors
- Extract DO class to `worker/mcp/index.ts`, re-export from `worker/index.ts` — wrangler types still resolve via `import("../worker/index").ClassName`
- postMessage runtime validation pattern: `isRecord()` guard + reconstruct typed object in `parseIframeMessage()` — avoids domain-level `as` casts on untrusted `event.data`
- Use `.js` extension for local imports (ESM convention with `moduleResolution: "bundler"`)
- `createUIResource` is sync — returns `UIResource` not `Promise`. Metadata keys prefixed `mcpui.dev/ui-`
- Streaming pipeline for iframe apps: `@cloudflare/kumo/streaming` (createJsonlParser, applyPatch) + `@cloudflare/kumo/generative` (UITreeRenderer). `readSSEStream` must be copied (not exported). CSS needs `@import "@cloudflare/kumo/styles"` + `@import "tailwindcss"` + `@source` directives
- React Router + Cloudflare Worker pattern: `reactRouter()` vite plugin + `createRequestHandler(() => import("virtual:react-router/server-build"))` as fallback in Worker fetch. Needs `strip-typegen-imports` plugin for `+types/` stubs, `types/react-router.d.ts` with `export =` for virtual module, `react-router.config.ts` with `ssr: true` + `v8_viteEnvironmentApi: true`
- MCP streamable HTTP requires `Accept: application/json, text/event-stream` — both needed or -32000 error
- Dual tsconfig for kumo-mcp: `tsconfig.json` (worker, no DOM) + `tsconfig.app.json` (iframe app, DOM + JSX). `typecheck` script runs both sequentially.
- postMessage settle pattern: wrap resolve/reject in `settle()` guard to prevent double-resolution from timeout vs message race

---
<!-- Task logs below - APPEND ONLY -->

## Task - scaffold-1
- Created `packages/kumo-mcp/` with Worker entrypoint, wrangler.jsonc, package.json, tsconfig.json, vite.config.ts
- Files: `worker/index.ts` (KumoPlaygroundMCP class + default fetch handler), `wrangler.jsonc` (DO binding + SQLite migration), `package.json` (agents, @modelcontextprotocol/sdk@1.23.0, @mcp-ui/server, @cloudflare/vite-plugin, zod), `tsconfig.json`, `vite.config.ts`, `types/worker-configuration.d.ts` (wrangler-generated)
- **Learnings:** `@modelcontextprotocol/sdk` version MUST match what `agents` pins (1.23.0) or McpServer types diverge. `@mcp-ui/server` wants `^1.25.1` which may cause issues in tool-1 — may need to relax SDK version or use older mcp-ui/server.

## Task - scaffold-2
- Extracted `KumoPlaygroundMCP` class from `worker/index.ts` to `worker/mcp/index.ts` following inspo pattern
- `worker/index.ts` now imports/re-exports class and routes `/mcp` to DO
- Added `ai` as dependency to resolve `agents` bundle-time dynamic import error
- Files changed: `worker/mcp/index.ts` (new), `worker/index.ts` (refactored), `package.json` (+ai dep)
- **Learnings:** `agents/mcp` entry point transitively bundles `client-QZa2Rq0l.js` which has `await import("ai")` — esbuild resolves this statically even though it's dynamic. Adding `ai` as a dep fixes the bundle error. The `@cloudflare/vite-plugin` v1.26.0 requires wrangler `^4.70.0` but monorepo pins `4.67.0`, so `vite dev` doesn't work — must use `wrangler dev` for now.

## Task - tool-1
- Created `worker/mcp/tools.ts` with `initializeTools(agent)` that registers `create_worker` tool
- Tool accepts `{ workerName: z.string() }`, returns `createUIResource` with `externalUrl` type pointing to `/ui/create-worker-confirm`
- `uiMetadata` includes `initial-render-data: { workerName, toolId }` and `preferred-frame-size: ["100%", "280px"]`
- Wired `initializeTools(this)` into `KumoPlaygroundMCP.init()`
- Verified via `wrangler dev` + curl: `tools/call` with `create_worker` returns correct UI resource with all metadata
- Files changed: `worker/mcp/tools.ts` (new), `worker/mcp/index.ts` (added import + init call)
- **Learnings:** `createUIResource` from `@mcp-ui/server` is synchronous (returns `UIResource`, not `Promise<UIResource>`) despite inspo using `await`. The `_meta` key prefix is `mcpui.dev/ui-` (e.g. `mcpui.dev/ui-initial-render-data`). MCP streamable HTTP transport requires `Accept: application/json, text/event-stream` header — omitting `text/event-stream` returns a `-32000` error.

## Task - tool-2
- Added `execute_create_worker` tool registration in `initializeTools` (worker/mcp/tools.ts)
- Tool accepts `{ workerName: z.string() }`, returns text content + `structuredContent: { success, workerName, createdAt }`
- Mock 800ms delay via `setTimeout` promise. `outputSchema` declared with Zod for structured output validation
- Verified via `wrangler dev` + curl: response includes both `content[0].text` and `structuredContent`, ~854ms round-trip
- Files changed: `worker/mcp/tools.ts` (added tool registration + delay helper)
- **Learnings:** `registerTool` supports `outputSchema` with Zod schemas for `structuredContent` validation. Async tool handlers work naturally with `McpAgent` — the DO handles the async correctly including `setTimeout` delays.

## Task - protocol-1
- Created `app/utils/mcp.ts` with `sendMcpMessage` and `waitForRenderData` exports
- `sendMcpMessage`: typed overloads per message type (tool/prompt/link), messageId-correlated via `crypto.randomUUID()`, Zod schema validation, timeout with cleanup
- `waitForRenderData`: posts `ui-lifecycle-iframe-ready`, awaits `ui-lifecycle-iframe-render-data`, Zod-validates render data, timeout with cleanup
- All message types modeled as discriminated unions: `OutboundMessage`, `InboundMessageResponse`, `InboundRenderData`, `LifecycleReadyMessage`, `SizeChangeMessage`
- Added `tsconfig.app.json` for iframe code (DOM + JSX libs), updated `package.json` typecheck to run both tsconfigs
- Files changed: `app/utils/mcp.ts` (new), `tsconfig.app.json` (new), `package.json` (typecheck script)
- **Learnings:** Worker code (no DOM) and iframe app code (DOM) need separate tsconfigs. `settle()` guard pattern prevents double-resolve race between timeout and message handler.

## Task - protocol-2
- Added `useMcpUiInit(rootRef)` hook to `app/utils/mcp.ts`
- On mount: posts `ui-lifecycle-iframe-ready` + reads `clientHeight`/`clientWidth` and posts `ui-size-change`
- Added `ResizeObserver` to report subsequent layout changes (not just initial), disconnects on unmount
- Helper `postToParent()` guards against top-level (non-iframe) context
- Added `react`, `react-dom` as deps + `@types/react`, `@types/react-dom` as devDeps (via `catalog:`)
- Files changed: `app/utils/mcp.ts` (added hook + import), `package.json` (+react deps)
- **Learnings:** Inspo hook has no cleanup — adding `ResizeObserver.disconnect()` in the effect teardown is essential for proper unmount. `useRef` for stable element ref avoids stale closure in observer callback.

## Task - protocol-3
- Created `packages/kumo-docs-astro/src/lib/mcp-host.ts` — host-side postMessage handler
- `createIframeMessageHandler(config)` factory: attaches `message` listener, returns `{ cleanup }` for unmount
- Runtime `parseIframeMessage()` validates raw `event.data` into typed discriminated union (`IframeMessage`) — no domain-level `as` casts
- Handles 3 message families: lifecycle ready → render data reply; action (tool/prompt/link) → callback + correlated response; size-change → resize callback
- Source filtering via `event.source !== iframe.contentWindow`
- Async callback support: sync and Promise results handled uniformly with `.then()`
- Types exported: `IframeToolMessage`, `IframePromptMessage`, `IframeLinkMessage`, `IframeActionMessage`, `SizeChangeMessage`, `RenderDataResponse`, `ActionResponse`, etc.
- Files changed: `src/lib/mcp-host.ts` (new)
- **Learnings:** postMessage `event.data` is `unknown` at runtime — use a `parseIframeMessage` validator that reconstructs typed objects rather than casting. Discriminated unions on `msg.type` let switch-case narrow payload types without assertions. `isRecord()` type guard (`value is Record<string, unknown>`) is the clean pattern for narrowing `unknown` → indexable object.

## Task - iframe-app-1
- Added React Router scaffold to `packages/kumo-mcp/` — full SSR iframe app served by Worker
- New files: `react-router.config.ts`, `app/routes.ts`, `app/root.tsx`, `app/entry.server.tsx`, `app/routes/ui/create-worker-confirm.tsx`, `types/react-router.d.ts`
- Updated: `vite.config.ts` (+reactRouter, +tsconfigPaths, +strip-typegen-imports plugin), `worker/index.ts` (createRequestHandler fallback for non-/mcp routes), `tsconfig.app.json` (+vite/client types, +rootDirs for .react-router/types), `package.json` (+react-router, +@react-router/dev, +isbot, +vite-tsconfig-paths)
- Route exports `clientLoader` → `waitForRenderData(renderDataSchema)` where schema validates `{ workerName, toolId }`, `HydrateFallback` with SVG spinner + "Waiting for render data...", default component renders confirmation card using `useLoaderData<RenderData>()`
- `useMcpUiInit(rootRef)` wired in default component for iframe dimension reporting
- **Learnings:** `virtual:react-router/server-build` module declaration needs `export = serverBuild` (CJS-style) for `() => import()` to resolve to `Promise<ServerBuild>` — `export default` wraps in a namespace object that doesn't satisfy the type. `strip-typegen-imports` Vite plugin needed to stub `+types/` imports that React Router generates at build time but don't exist on disk during dev/typecheck.

## Task - iframe-app-2
- Implemented `streamConfirmationCard` function in `app/utils/stream-confirmation-card.ts` — fetches `/api/chat` with `skipSystemPrompt: true` + `systemPromptOverride` set to `TOOL_CONFIRMATION_PROMPT`
- Response parsed via `readSSEStream` → `createJsonlParser().push(token)` → `applyPatch(tree, op)` pipeline
- Copied `readSSEStream` to `app/utils/read-sse-stream.ts` (standalone utility, not exported from any package)
- Updated `create-worker-confirm.tsx` route: on mount streams card via `streamConfirmationCard`, renders progressively with `UITreeRenderer` (`streaming=true` during load), transitions to `pending` on complete
- Added `@cloudflare/kumo` (`workspace:*`) + `@phosphor-icons/react` as deps for streaming/generative imports
- Added `@tailwindcss/vite` + `tailwindcss` as devDeps, wired into `vite.config.ts`
- Created `app/app.css` with `@cloudflare/kumo/styles` + `tailwindcss` imports, imported in `root.tsx`
- `handleAction` callback typed as `ActionDispatch` from `@cloudflare/kumo/streaming`, handles `tool_approve` → `applying` and `tool_cancel` → `cancelled` (actual MCP messaging deferred to iframe-app-3)
- Error state renders fallback if stream fails without partial content; partial tree shown in pending state
- Files changed: `app/utils/stream-confirmation-card.ts` (new), `app/utils/read-sse-stream.ts` (new), `app/app.css` (new), `app/routes/ui/create-worker-confirm.tsx` (rewritten), `app/root.tsx` (+css import), `vite.config.ts` (+tailwindcss plugin), `package.json` (+kumo, +phosphor, +tailwind deps)
- **Learnings:** `@cloudflare/kumo/streaming` exports `createJsonlParser`, `applyPatch`, `EMPTY_TREE`, `ActionDispatch` — all needed for iframe streaming. `@cloudflare/kumo/generative` exports `UITreeRenderer`. `readSSEStream` is a docs-site utility, not exported from any package — needs to be copied. Kumo CSS requires both `@cloudflare/kumo/styles` and `tailwindcss` imports with `@source` directives pointing to kumo dist.

## Task - iframe-app-3
- Wired `tool_approve` action → `sendMcpMessage("tool", { toolName: "execute_create_worker", params: { workerName } }, { schema: executeResultSchema })` with Zod validation of `{ structuredContent: { success, workerName, createdAt } }`
- On success (`structuredContent.success === true`): status → `completed` with green CheckIcon overlay
- On failure or error: reverts to `pending` so user can retry
- Wired `tool_cancel` action → `sendMcpMessage("tool", { toolName: "cancel_tool", params: { toolId } })` fire-and-forget + status → `cancelled`
- Status-based overlays: `applying` = semi-transparent white overlay + animated spinner, `completed` = overlay + green check circle, `cancelled` = card opacity 0.5 + pointer-events-none
- Only `pending` status allows button interaction (pointer-events-none on all other states)
- Inline SVG components (StatusOverlay, Spinner, CheckIcon) — avoids @phosphor-icons/react bundle cost
- Files changed: `app/routes/ui/create-worker-confirm.tsx` (added sendMcpMessage import, executeResultSchema, action wiring, overlay rendering, 3 helper components)
- **Learnings:** `sendMcpMessage` already handles messageId correlation + timeout + Zod validation — just need to provide schema and handle the promise. Cancel is best-effort (host may not register a `cancel_tool` handler, so `.catch(() => {})` swallows the error). Inline SVGs keep bundle small vs importing icon libraries for 2 icons.

## Task - host-1
- Created `McpToolIframe` React component at `packages/kumo-docs-astro/src/components/McpToolIframe.tsx`
- Props: `src`, `renderData`, `status` (ToolIframeStatus), `onToolAction`, `onStatusChange`
- Renders `<iframe sandbox="allow-scripts allow-same-origin">` with dynamic height from `onResize` callback
- Sets up `createIframeMessageHandler` on mount → handles render data delivery, tool message forwarding, size changes; cleans up listener on unmount
- Callback ref pattern (`onToolActionRef`) avoids re-running the effect when parent re-renders with new inline closures
- Status-based CSS matches existing `_PlaygroundPage.tsx` patterns: streaming = loader placeholder + hidden iframe, cancelled = opacity-50 + line-through + pointer-events-none, applying = semi-transparent overlay + Loader, completed = overlay + CheckIcon + "Applied"
- Streaming state renders a placeholder above the iframe (which is `sr-only` + height:0); once status transitions past streaming, iframe becomes visible at reported height
- Exported types: `ToolIframeStatus`, `ToolActionPayload`, `McpToolIframeProps`
- Files changed: `src/components/McpToolIframe.tsx` (new)
- **Learnings:** `createIframeMessageHandler` handles the render-data lifecycle internally — just pass `renderData` in config and the handler replies to `ui-lifecycle-iframe-ready` automatically. The `onToolCall` return value becomes the correlated `ui-message-response` payload — returning `undefined` works for now since the host-2 MCP proxy will provide real responses. Astro `~/` path alias works at build time even if LSP shows errors — `astro check` is the authoritative typecheck.
