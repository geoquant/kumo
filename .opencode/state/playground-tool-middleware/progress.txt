# Progress Log
PRD: playground-tool-middleware
Started: 2026-03-04
**CRITICAL: All work MUST be on branch `geoquant/streaming-ui`. Do NOT use `playground-tool-middleware` branch.**

## Codebase Patterns
- ChatMessage is discriminated union on `role` field — narrow before accessing role-specific fields
- `TextChatMessage` has `content`; `ToolChatMessage` has `toolId`, `tree`, `status`
- UITree type from `@cloudflare/kumo/streaming` (imported at line 92)
- Action system: ActionEvent → ActionDispatch → handlers (action-types.ts, action-registry.ts)
- Streaming pipeline: readSSEStream → createJsonlParser → useUITree → UITreeRenderer
- Pre-existing typecheck errors in shiki/echarts modules — ignore
- `streamPanelB` sends history to API — must filter to TextChatMessage only
- Chat message rendering at ~line 2170, must handle all 3 roles explicitly
- JSONL generator pattern: build `readonly object[]` of RFC 6902 ops, `JSON.stringify` each, `.join("\n")`; feed to parser with trailing `\n` or use `flush()`
- Custom action handlers: define `ActionHandler` functions → `ActionHandlerMap` → `createHandlerMap(custom)` merges with BUILTIN_HANDLERS; use merged map in `dispatchAction`
- Tool card rendering: `UITreeRenderer` with `onAction` prop directly in chat loop; no wrapper bubble chrome; `w-full overflow-hidden` prevents sidebar overflow
- Tool action dispatch: `handleToolAction` finds tool message by `event.params.toolId`, passes its `tree` to `dispatchAction`; uses `messagesRef` to avoid stale closure
- Status-based tool card styling: CSS wrapper with `cn()` around UITreeRenderer; `opacity-50 pointer-events-none` for disabled; `onAction={undefined}` to suppress dispatch; Text heading variants render as `<h1>`/`<h2>`/`<h3>` not `<span>`

---
<!-- Task logs below - APPEND ONLY -->

## Task - types-1
- Extended ChatMessage from single interface to discriminated union: TextChatMessage | ToolChatMessage
- Added ToolMessageStatus type: 'pending' | 'approved' | 'cancelled' | 'applying' | 'completed'
- ToolChatMessage carries: toolId, tree (UITree), status (ToolMessageStatus)
- Updated message rendering to narrow on all 3 roles (tool renders `null` placeholder for now)
- Filtered tool messages from API history (streamPanelB + baseBody)
- Files changed: _PlaygroundPage.tsx
- **Learnings:** Discriminated union requires explicit narrowing at all usage sites; the `else` branch of `role === "assistant"` previously assumed user, now must check explicitly

## Task - generator-1
- Added `generateCreateWorkerConfirmation(workerName)` function that returns JSONL (newline-delimited RFC 6902 patches)
- Builds UITree: Surface → Text heading ("Verify this change") → Cluster (Text + Badge workerName) → Stack (Cancel + Approve buttons)
- Cancel button: variant outline, action `tool_cancel` with `params.toolId`
- Approve button: variant primary, action `tool_approve` with `params.toolId`
- toolId format: `create-worker-{workerName}`
- End-to-end verified: JSONL → createJsonlParser → applyPatch → correct UITree with 8 elements
- Files changed: _PlaygroundPage.tsx
- **Learnings:** JSONL generator is just JSON.stringify per line joined with \n; parsePatchLine needs trailing \n for push() to emit (or use flush()); `readonly object[]` type is sufficient for the lines array

## Task - actions-1
- Added `handleToolApprove` and `handleToolCancel` action handlers returning `MessageResult` with JSON content
- `tool_approve` → `{ toolId, approved: true }`, `tool_cancel` → `{ toolId, approved: false }`
- Created `TOOL_ACTION_HANDLERS` map and merged via `createHandlerMap` into `PLAYGROUND_HANDLERS`
- Updated both panel action dispatchers (`handleAction`, `handleNoPromptAction`) to use `PLAYGROUND_HANDLERS` instead of `BUILTIN_HANDLERS`
- Removed direct `BUILTIN_HANDLERS` import �� `createHandlerMap` includes built-ins automatically
- Files changed: _PlaygroundPage.tsx
- **Learnings:** `createHandlerMap(custom)` merges custom over built-in; playground can define domain-specific handlers without touching `@cloudflare/kumo` package; handlers that don't need the tree can ignore the second param

## Task - ui-1
- Tool messages now render inline `UITreeRenderer` in chat sidebar (no AssistantMessageSummary)
- Added `onToolAction` prop to `PlaygroundChatSidebar` for button dispatch
- Created `handleToolAction` in `PlaygroundContent` — finds tool message by `toolId` from `event.params`, dispatches via `PLAYGROUND_HANDLERS`
- Uses `messagesRef` (useRef synced to messages state) to avoid stale closure in useCallback
- Tool card wrapped in `w-full overflow-hidden` to fit 380px sidebar without horizontal overflow
- No status chrome above the card — raw UITreeRenderer output
- Files changed: _PlaygroundPage.tsx
- **Learnings:** `UITreeRenderer.onAction` is `ActionDispatch` = `(event: ActionEvent) => void`; tree must be looked up separately since renderer doesn't pass it; `messagesRef` pattern avoids re-creating handler on every message change

## Task - ui-2
- Cancelled tool messages now render greyed out: `opacity-50` + `pointer-events-none`
- Strikethrough on action text via `[&_h1]:line-through [&_h2]:line-through [&_h3]:line-through [&_span]:line-through`
- Buttons non-interactive: `pointer-events-none` on container + `onAction={undefined}` when cancelled
- Added `cn` import from `@cloudflare/kumo` for conditional className composition
- Files changed: _PlaygroundPage.tsx
- **Learnings:** UITreeRenderer has no `disabled` prop — status-based visual states must be applied via CSS wrapper; Text heading3 renders as `<h3>` not `<span>`, so line-through selectors need heading tags too; `cn()` from kumo wasn't previously imported in PlaygroundPage
