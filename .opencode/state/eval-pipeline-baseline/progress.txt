# Progress Log
PRD: eval-pipeline-baseline
Started: 2026-02-28

## Codebase Patterns
- Dist types staleness: when kumo `src/generative/index.ts` adds new exports, must `pnpm --filter @cloudflare/kumo build` before docs-astro typecheck passes
- CLI eval script: `packages/kumo/scripts/eval-generative.ts` — standalone tsx script, not part of library build
- Eval prompts source of truth: `packages/kumo/src/generative/eval/eval-prompts.ts` — exports `EVAL_PROMPTS`, `EvalPrompt` type
- CLI uses local `EvalPrompt` interface with `name`/`message` fields; shared module uses `id`/`prompt` fields
- Composition graders: `packages/kumo/src/generative/composition-graders.ts` — exports `gradeComposition`, `COMPOSITION_RULE_NAMES`
- Generative barrel: `packages/kumo/src/generative/index.ts` — already exports composition graders
- Structural graders export `RULE_NAMES`, `GradeReport`, `gradeTree`, `parseJsonlToTree`
- Baseline JSON backward compat: read old baselines as `Partial<Baseline>`, check for new fields before comparing
- Skills API: GET `/api/chat/skills` requires `X-Playground-Key` header, returns `{ skills: [{ id, name, description }] }`
- Chat API: POST `/api/chat` accepts optional `skillIds` string[] (max 5) and `X-Playground-Key` header; skills only effective when auth=authenticated
- SVG fill tokens: `fill-kumo-subtle` / `fill-kumo-default` don't exist; use `text-kumo-*` + `fill="currentColor"` for SVG text
- Astro nested routing: moving `foo.astro` to `foo/index.astro` requires updating all relative imports (one extra `../`)
- Playground uses `BaseLayout` directly (no sidebar); report page follows same pattern

---
<!-- Task logs below - APPEND ONLY -->

## Task - d1-unify-prompts
- Removed hardcoded `EVAL_PROMPTS` (10 prompts) and local `EvalPrompt` interface from `eval-generative.ts`
- Imported `EVAL_PROMPTS` from `../src/generative/eval/eval-prompts.js` (13 prompts)
- Adapted all references: `prompt.name` → `prompt.id`, `prompt.message` → `prompt.prompt`
- Widened report table column from 22 to 32 chars to accommodate longer prompt IDs
- Files changed: `packages/kumo/scripts/eval-generative.ts`
- **Learnings:** CLI script is a standalone tsx script not included in the library build; the shared `EvalPrompt` type uses `id`/`prompt` while the CLI was using `name`/`message`

## Task - d2-cli-composition-grading
- Imported `gradeComposition` and `COMPOSITION_RULE_NAMES` from `composition-graders.js`
- Added `compositionReport: GradeReport | null` field to `RunResult`
- Added `compositionPassRates: Record<string, number>` to `PromptAggregate`
- Added `overallComposition: Record<string, number>` to `Baseline`
- Call `gradeComposition(tree)` immediately after `gradeTree(tree)` for each run
- `allPass` now requires both structural AND composition reports to pass
- Report table split into two sections: Structural (8 rules) + Composition (6 rules)
- Baseline save persists `overallComposition` alongside existing `overall`
- Comparison reads old baselines as `Partial<Baseline>` — missing `overallComposition` renders as "N/A" per rule
- Files changed: `packages/kumo/scripts/eval-generative.ts`
- **Learnings:** `GradeReport` type is shared between structural and composition graders; old baselines won't have `overallComposition` so `Partial<Baseline>` + null checks give backward compat

## Task - d3-skills-flag
- Added `skills: boolean` and `playgroundKey: string | null` to `CliArgs`
- `--playground-key` falls back to `PLAYGROUND_SECRET` env var
- `--skills` without a key prints error and exits with code 1
- Added `fetchSkillIds()`: GET `/api/chat/skills` with `X-Playground-Key` header, extracts `skills[].id`
- Added `FetchJsonlOptions` interface; `fetchJsonl()` now accepts optional `playgroundKey` + `skillIds`
- When `--skills` set: header `X-Playground-Key` + body `skillIds` array sent with every request
- Without `--skills`: requests are unchanged (no auth header, no skillIds)
- `Baseline.args` omits `skills`/`playgroundKey` — baseline JSON shape identical for skills vs no-skills
- Renamed inner `body` variable to `responseBody` to avoid shadowing the request body variable
- Files changed: `packages/kumo/scripts/eval-generative.ts`
- **Learnings:** Skills endpoint at `/api/chat/skills` requires GET + `X-Playground-Key`; chat endpoint accepts optional `skillIds` array (max 5) and `X-Playground-Key` header; skills only effective when authenticated

## Task - d4-playground-composition-ui
- Imported `gradeComposition` and `COMPOSITION_RULE_NAMES` from `@cloudflare/kumo/generative`
- Added `COMPOSITION_RULE_DESCRIPTIONS` map with human-readable descriptions for all 6 composition rules
- Combined into `RULE_DESCRIPTIONS` via spread for shared lookup
- Added `compositionReport` state (`GradeReport | null`) to `GradingTabContent`
- `runGrade` callback now calls both `gradeTree()` and `gradeComposition()` in sequence — debouncing preserved
- Extracted `GradeSection` component for rendering a titled group of grade results (DRY)
- Score summary shows combined pass count across both structural and composition (e.g. "12/14 rules passing")
- Two labeled sections: "Structural (6/8)" and "Composition (5/6)"
- Had to rebuild kumo package (`pnpm --filter @cloudflare/kumo build`) because dist types were stale — composition grader exports were in source `index.ts` but not in `dist/src/generative/index.d.ts`
- Files changed: `packages/kumo-docs-astro/src/components/demos/_PlaygroundPage.tsx`
- **Learnings:** Dist types can go stale when source barrel adds new re-exports; must rebuild kumo before docs-astro typecheck will pass. `GradeReport` type from structural-graders is reused by composition-graders (same shape), so both reports can share rendering logic.

## Task - d6-eval-report-page
- Created `/playground/report` eval report page with full-page React island
- Moved `playground.astro` → `playground/index.astro` for Astro nested routing (fixed relative imports)
- Created `playground/report.astro` wrapper using `BaseLayout` (same pattern as playground)
- Created `_EvalReportPage.tsx` (1157 lines) — complete eval report page:
  - **Live eval mode** (authenticated): runs all 13 EVAL_PROMPTS sequentially via `/api/chat` SSE, grades progressively with `gradeTree()` + `gradeComposition()`, builds baseline from results
  - **Upload mode** (anyone): drag-drop baseline JSON files for viewing
  - **Pass/fail matrix**: 13 prompts × 14 rules (8 structural + 6 composition) grid with ✓/✗ indicators
  - **A/B comparison**: side-by-side per-rule pass rates with signed delta badges when 2 baselines selected
  - **Trend chart**: SVG polyline chart of overall pass rate across 3+ baselines
  - **Auth gating**: unauthenticated users see upload-only mode with info banner; authenticated users get live eval + upload
  - Baseline list with select/deselect for comparison, remove buttons
- Re-exported `EVAL_PROMPTS` and `EvalPrompt` type from `@cloudflare/kumo/generative` barrel
- Rebuilt kumo package to refresh dist types
- Files changed:
  - `packages/kumo/src/generative/index.ts` (added EVAL_PROMPTS re-export)
  - `packages/kumo-docs-astro/src/pages/playground.astro` → `playground/index.astro` (moved, fixed imports)
  - `packages/kumo-docs-astro/src/pages/playground/report.astro` (new)
  - `packages/kumo-docs-astro/src/components/demos/_EvalReportPage.tsx` (new)
- **Learnings:** SVG `fill-kumo-*` tokens don't exist in theme; use `text-kumo-*` + `fill="currentColor"`. Moving Astro pages into subdirectories breaks relative imports — need to add extra `../`. `readSSEStream` and `createJsonlParser` from streaming module work well for client-side eval; `parseJsonlToTree` from generative barrel handles JSONL→UITree conversion.
