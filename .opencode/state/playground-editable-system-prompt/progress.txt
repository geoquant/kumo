# Progress Log
PRD: playground-editable-system-prompt
Started: 2026-03-12

## Codebase Patterns
- lz-string ships bundled types (typings/lz-string.d.ts), no @types package needed
- pnpm --filter @cloudflare/kumo-docs-astro add <pkg> for docs-only deps
- Pre-existing typecheck warnings (unused vars in .astro files) — ignore, not ours
- eslint enforces `_` prefix for unused vars; rename back when wiring into consumers
- PlaygroundContent state lives in `_PlaygroundPage.tsx:508+`; systemPromptText at ~855, editable prompt state at ~872
- URL sync hook at `src/lib/playground/prompt-url-sync.ts`; uses lz-string `compressToEncodedURIComponent` (URL-safe, no extra encoding needed)
- Badge component: `variant` only (primary|secondary|destructive|outline|success|beta), no `size` prop
- PromptEditor at `_PlaygroundPage.tsx:~4497` (now wired; renders in panel A prompt tab)
- PanelContent takes `panelId: PanelId` as required prop; prompt-editor props are optional (panel B doesn't need them)
- ComparisonPanelsProps is the interface for threading props from PlaygroundContent → ComparisonPanels → PanelContent
- Mobile shell (`MobilePlaygroundShell`) also has PanelContent calls — needs `panelId` but NOT prompt-editor props (mobile doesn't wire editable prompt yet)

---

## Task - setup-1
- Installed lz-string ^1.5.0 as dependency of kumo-docs-astro
- Files changed: packages/kumo-docs-astro/package.json, pnpm-lock.yaml
- **Learnings:** lz-string has bundled types; compressToEncodedURIComponent / decompressFromEncodedURIComponent are the URL-safe methods

## Task - state-1
- Added `editedSystemPrompt` useState<string | null> (init null) to PlaygroundContent
- Added `isPromptModified` derived boolean (editedSystemPrompt !== null)
- Added `activeSystemPrompt` derived value (editedSystemPrompt ?? systemPromptText)
- Files changed: packages/kumo-docs-astro/src/components/demos/_PlaygroundPage.tsx
- **Learnings:** Codebase uses `_` prefix for declared-but-unused vars (see `_appliedSkillIds`). Prefixed setter/derived values with `_` since they're unused until wired in later tasks. eslint `no-unused-vars` rule requires this.

## Task - state-2
- Created `usePromptUrlSync` hook in `src/lib/playground/prompt-url-sync.ts`
- On mount: reads `?promptOverride=` (lz-string decompress) or `?promptRef=` (localStorage lookup) → sets editedSystemPrompt
- On change: debounced (400ms) replaceState; compressed ≤ 1500 chars → `?promptOverride=`, else localStorage + `?promptRef=<hash>`
- On null: removes both params from URL
- Uses FNV-1a 32-bit hash for localStorage keys (deterministic, fast)
- Wired into PlaygroundContent; renamed `_setEditedSystemPrompt` → `setEditedSystemPrompt` since it's now consumed
- Files changed: `src/lib/playground/prompt-url-sync.ts` (new), `src/components/demos/_PlaygroundPage.tsx`
- **Learnings:** `replaceState` preserves scroll position and doesn't trigger navigation; debouncing prevents URL thrash during rapid edits. `compressToEncodedURIComponent` is already URL-safe (no encodeURIComponent needed).

## Task - ui-1
- Created `_PromptEditor` component in `_PlaygroundPage.tsx` (prefixed `_` until wired in ui-2)
- Monospace `<textarea>` pre-filled with `canonicalPrompt` when `editedPrompt` is null
- Header: token count estimate + `Badge variant="outline"` "modified" indicator (shown when `isModified`)
- Footer: "Regenerate with this prompt" `Button variant="outline"` with `ArrowCounterClockwiseIcon`, disabled when `isStreaming`
- Added `Badge` to `@cloudflare/kumo` imports
- All semantic tokens: `bg-kumo-base`, `text-kumo-default`, `border-kumo-line`, `text-kumo-subtle`
- Files changed: `src/components/demos/_PlaygroundPage.tsx`
- **Learnings:** Badge has no `size` prop, only `variant`. Available variants: primary, secondary, destructive, outline, success, beta. No `warning` variant — use `outline` for informational badges. Function declarations also need `_` prefix for eslint unused-vars rule.

## Task - ui-2
- Added `panelId: PanelId` prop to `PanelContent`; conditional render in prompt tab: panel A → `PromptEditor`, panel B → `PromptTextView`
- Added `editedSystemPrompt`, `isPromptModified`, `onPromptChange`, `onPromptReset`, `onRegenerate` to `ComparisonPanelsProps`
- Threaded all new props: PlaygroundContent → ComparisonPanels → PanelContent (desktop + mobile)
- Created stable callbacks: `handlePromptChange`, `handlePromptReset`, `handleRegenerate` (placeholder no-op for functional-1)
- Renamed `_PromptEditor` → `PromptEditor`, un-prefixed `isPromptModified` (now consumed)
- `activeSystemPrompt` stays `_`-prefixed until consumed by functional-1
- `onPromptReset` aliased as `_onPromptReset` in PanelContent destructuring (unused until PromptEditor grows a reset button)
- Files changed: `src/components/demos/_PlaygroundPage.tsx`
- **Learnings:** PromptEditor props are optional on PanelContent (panel B doesn't need them). Guard with runtime check (`panelId === "a" && onPromptChange && onRegenerate`) to narrow optional props before render.
