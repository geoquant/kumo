# Progress Log
PRD: playground-iteration-inspection-parity
Started: 2026-03-08

## Codebase Patterns
- Verify the current branch is `geoquant/streaming-ui` before starting any new work for this PRD; do not branch away for playground parity tasks.
- Keep shared playground state shapes in `packages/kumo-docs-astro/src/lib/playground/types.ts` and pure state transitions in `packages/kumo-docs-astro/src/lib/playground/state.ts`.
- Migrate mirrored panel UI state first (tabs, status, raw JSONL, action logs, layout flags) and leave stream/runtime hooks intact until later tasks need deeper consolidation.
- For inspection tabs, derive a memoized JSON-friendly view model in `src/lib/playground/` and render it through `HighlightedCode` with the same copy-button pattern used by JSONL and TSX tabs.
- When an editor can override streamed output, keep the streamed tree and local override separate; render `override ?? streamTree` and only patch the override locally when interactive actions fire.
- Keep catalog reference data UI-agnostic: normalize registry components and action definitions in `src/lib/playground/catalog-data.ts`, then let later UI layers consume sorted arrays instead of raw registry maps.
- For shared explorers, anchor open/close state in the top-level layout reducer and render one responsive overlay that becomes a desktop sheet and a mobile full-screen view instead of duplicating separate desktop/mobile components.

---

## Task - architecture-1
- Implemented shared playground panel/layout/editor types in `packages/kumo-docs-astro/src/lib/playground/types.ts`.
- Implemented pure panel/layout reducer helpers in `packages/kumo-docs-astro/src/lib/playground/state.ts`.
- Updated `packages/kumo-docs-astro/src/components/demos/_PlaygroundPage.tsx` to consume reducer-backed panel/layout state for tabs, stream status, raw JSONL, action logs, and chat minimization.
- Files changed: `packages/kumo-docs-astro/src/components/demos/_PlaygroundPage.tsx`, `packages/kumo-docs-astro/src/lib/playground/state.ts`, `packages/kumo-docs-astro/src/lib/playground/types.ts`, `.opencode/state/playground-iteration-inspection-parity/prd.json`, `.opencode/state/playground-iteration-inspection-parity/progress.txt`
- **Learnings:** introduce pure reducers before adding new tabs or layouts; this removes `foo` / `noPromptFoo` state drift without disturbing the working stream lifecycle.

## Task - ui-1
- Added a new `Tree` panel tab to the playground tab set for both A and B panels.
- Implemented `packages/kumo-docs-astro/src/lib/playground/nested-tree.ts` to derive a nested `UITree` view model with stable keys, types, props, action metadata, and recursive children.
- Added `TreeTabContent` to `packages/kumo-docs-astro/src/components/demos/_PlaygroundPage.tsx` with live updates during streaming, copy-to-clipboard support, and empty/loading states.
- Files changed: `packages/kumo-docs-astro/src/components/demos/_PlaygroundPage.tsx`, `packages/kumo-docs-astro/src/lib/playground/nested-tree.ts`, `packages/kumo-docs-astro/src/lib/playground/types.ts`, `.opencode/state/playground-iteration-inspection-parity/prd.json`, `.opencode/state/playground-iteration-inspection-parity/progress.txt`
- **Learnings:** nested inspection should not depend on `isRenderableTree()`; deriving from the raw `UITree` lets the debug view appear earlier during partial streaming.

## Task - validation-1
- Added a new `Editor` tab to both playground panels with `Format`, `Validate`, `Apply`, and `Reset to latest streamed tree` controls.
- Implemented `packages/kumo-docs-astro/src/lib/playground/validate-tree.ts` to parse edited JSON and validate `UITree` shape through Kumo catalog validation before apply.
- Extended playground panel state to track local tree overrides separately from streamed trees so manual edits update preview, TSX, grading, and tree views without triggering a new request.
- Routed interactive action patches against local overrides when present so edited previews remain internally consistent.
- Files changed: `packages/kumo-docs-astro/src/components/demos/_PlaygroundPage.tsx`, `packages/kumo-docs-astro/src/lib/playground/state.ts`, `packages/kumo-docs-astro/src/lib/playground/types.ts`, `packages/kumo-docs-astro/src/lib/playground/validate-tree.ts`, `.opencode/state/playground-iteration-inspection-parity/prd.json`, `.opencode/state/playground-iteration-inspection-parity/progress.txt`
- **Learnings:** validation helpers should cache an initialized catalog instance, and editor reset should clear local overrides before the next streamed generation starts.

## Task - data-1
- Added `packages/kumo-docs-astro/src/lib/playground/catalog-data.ts` to normalize component metadata from `virtual:kumo-registry` into sorted catalog entries with prop metadata.
- Added explicit normalized action entries for built-in actions (`increment`, `decrement`, `reset`, `set`, `toggle`, `submit_form`, `navigate`) plus playground-specific actions (`tool_approve`, `tool_cancel`).
- Exported sorted component categories alongside normalized component/action arrays so the later catalog UI can stay focused on rendering and filtering.
- Files changed: `packages/kumo-docs-astro/src/lib/playground/catalog-data.ts`, `.opencode/state/playground-iteration-inspection-parity/prd.json`, `.opencode/state/playground-iteration-inspection-parity/progress.txt`
- **Learnings:** registry normalization should follow existing docs requiredness logic (`required === true || optional === false`) so later UIs stay consistent with `PropsTable` and CLI docs.

## Task - ui-2
- Added a shared `Catalog` trigger to the playground workspace header in `packages/kumo-docs-astro/src/components/demos/_PlaygroundPage.tsx`.
- Implemented a single responsive catalog explorer overlay that renders as a right-hand sheet on desktop and a full-screen catalog view on mobile.
- Added search-by-name, category filtering for components, and segmented switching between component and action reference data.
- Rendered normalized component cards with description, import path, prop types, and requiredness plus action cards with descriptions and parameter metadata.
- Files changed: `packages/kumo-docs-astro/src/components/demos/_PlaygroundPage.tsx`, `.opencode/state/playground-iteration-inspection-parity/prd.json`, `.opencode/state/playground-iteration-inspection-parity/progress.txt`
- **Learnings:** a shared overlay satisfies both desktop sheet and mobile top-level catalog needs without polluting the per-panel tab model.
